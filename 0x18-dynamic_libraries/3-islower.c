#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c: is int
 * main - checks for lowercase character
 *Return: Always 0 (Success)
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
