#include "holberton.h"

/**
 * puts2 - print only one character out of two
 * @str: input
 * Return: number
 */
void puts2(char *str)
{
	int c;
	
	for (c = 0; c <= str; c++)
	{
		if (c % 2 == 0)
		{
			_putchar (c + '0');
		}
	}
	_putchar('\n');
}
