#include "holberton.h"
#include <stdio.h>

/**
 * _puts - returns the length of a string
 * @str: string
 *
 * Return: the length of the string
 */
void _puts(char *str)
{
	printf("%s",str);
	_putchar('\n');
}
