#include "holberton.h"
#include <stdio.h>

/**
 * reset_to_98 - takes a pointer and updates the value it points to to 98
 *
 * @n: the int that will be updated
 */
void swap_int(int *a, int *b)
{
	*a = 42;
	*b = 98;
}
