#include "holberton.h"
#include <stdio.h>

/**
 * puts_half - print only one character out of two
 * @str: input
 * Return: number
 */
void print_array(int *a, int n)
{
	printf("%d", *a);
	printf("%d", n);
}
